<!DOCTYPE html>
<html>
<head><meta charset="utf-8"><title>New User</title></head>
<body>
    <h1>Create User</h1>
    <p><a href="{{ path('user_index') }}">Back to list</a></p>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Create</button>
    {{ form_end(form) }}
</body>
</html>
