{% extends "base.html.twig" %}

{% block title %}Edit News - CarthageGG{% endblock %}

{% block content %}
<section class="py-12 px-6 bg-gradient-to-b from-[#0a0a0f] to-[#1a1a24]">
    <div class="container mx-auto max-w-2xl">
        <!-- Back Button -->
        <a href="{{ path('app_news_list') }}" class="inline-flex items-center text-[#D4AF37] hover:text-[#B8941F] transition mb-8">
            ← Back to News
        </a>

        <!-- Form Header -->
        <div class="mb-8">
            <h1 class="text-4xl font-bold text-white mb-2">Edit News Article</h1>
            <p class="text-gray-400">Update the news article details</p>
        </div>

        <!-- Error Messages -->
        {% if errors %}
            <div class="mb-6 p-4 bg-red-500/10 border border-red-500/20 rounded-lg">
                <h3 class="text-red-500 font-bold mb-2">Please fix the following errors:</h3>
                <ul class="list-disc list-inside text-red-400 space-y-1">
                    {% for field, message in errors %}
                        <li>{{ message }}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}

        <!-- Success Message -->
        {% if success %}
            <div class="mb-6 p-4 bg-green-500/10 border border-green-500/20 rounded-lg text-green-500">
                ✓ {{ successMessage ?? 'News article updated successfully!' }}
            </div>
        {% endif %}

        <!-- Form -->
        <form action="{{ path('app_news_edit', {'id': news.newsId}) }}" method="POST" enctype="multipart/form-data" class="bg-[#12121a] rounded-xl border border-[#D4AF37]/10 p-8">
            <!-- Title Field -->
            <div class="mb-6">
                <label for="titre" class="block text-sm font-semibold text-white mb-2">Title *</label>
                <input 
                    type="text" 
                    id="titre" 
                    name="titre" 
                    value="{{ news.titre }}" 
                    placeholder="Enter article title" 
                    class="w-full px-4 py-3 bg-[#1a1a24] border border-[#D4AF37]/20 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:border-[#D4AF37] transition"
                >
                {% if errors.title %}<p class="text-red-400 text-sm mt-1">{{ errors.title }}</p>{% endif %}
            </div>

            <!-- Category Field -->
            <div class="mb-6">
                <label for="categorie" class="block text-sm font-semibold text-white mb-2">Category *</label>
                <select 
                    id="categorie" 
                    name="categorie"
                    class="w-full px-4 py-3 bg-[#1a1a24] border border-[#D4AF37]/20 rounded-lg text-white focus:outline-none focus:border-[#D4AF37] transition"
                >
                    <option value="">Select a category</option>
                    <option value="tournaments" {% if news.categorie == 'tournaments' %}selected{% endif %}>Tournaments</option>
                    <option value="matches" {% if news.categorie == 'matches' %}selected{% endif %}>Matches</option>
                    <option value="players" {% if news.categorie == 'players' %}selected{% endif %}>Players</option>
                    <option value="events" {% if news.categorie == 'events' %}selected{% endif %}>Events</option>
                    <option value="updates" {% if news.categorie == 'updates' %}selected{% endif %}>Updates</option>
                    <option value="announcements" {% if news.categorie == 'announcements' %}selected{% endif %}>Announcements</option>
                    <option value="results" {% if news.categorie == 'results' %}selected{% endif %}>Results</option>
                    <option value="statistics" {% if news.categorie == 'statistics' %}selected{% endif %}>Statistics</option>
                </select>
                {% if errors.category %}<p class="text-red-400 text-sm mt-1">{{ errors.category }}</p>{% endif %}
            </div>

            <!-- Content Field -->
            <div class="mb-6">
                <label for="contenu" class="block text-sm font-semibold text-white mb-2">Content *</label>
                <textarea 
                    id="contenu" 
                    name="contenu" 
                    placeholder="Write your news article here..." 
                    rows="10"
                    class="w-full px-4 py-3 bg-[#1a1a24] border border-[#D4AF37]/20 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:border-[#D4AF37] transition resize-none"
                >{{ news.contenu }}</textarea>
                {% if errors.content %}<p class="text-red-400 text-sm mt-1">{{ errors.content }}</p>{% endif %}
            </div>

            <!-- Image Field -->
            <div class="mb-8">
                <label for="image" class="block text-sm font-semibold text-white mb-2">Featured Image (Optional)</label>
                
                <!-- Current Image Preview -->
                {% if news.image %}
                    <div class="mb-4 p-4 bg-[#1a1a24] rounded-lg border border-[#D4AF37]/10">
                        <p class="text-sm text-gray-400 mb-2">Current Image:</p>
                        <img src="{{ asset('uploads/news/' ~ news.image) }}" alt="{{ news.titre }}" class="max-h-48 rounded">
                    </div>
                {% endif %}

                <div class="relative">
                    <input 
                        type="file" 
                        id="image" 
                        name="image" 
                        accept="image/*"
                        class="w-full px-4 py-3 bg-[#1a1a24] border border-[#D4AF37]/20 rounded-lg text-white focus:outline-none focus:border-[#D4AF37] transition cursor-pointer"
                    >
                </div>
                <p class="text-gray-500 text-sm mt-2">Leave empty to keep the current image</p>
                {% if errors.image %}<p class="text-red-400 text-sm mt-1">{{ errors.image }}</p>{% endif %}
            </div>

            <!-- Submit Button -->
            <div class="flex gap-4">
                <button 
                    type="submit" 
                    class="flex-1 px-6 py-3 bg-[#D4AF37] text-[#0a0a0f] rounded-lg font-bold text-lg hover:bg-[#B8941F] transition"
                >
                    Update News
                </button>
                <a 
                    href="{{ path('app_news_detail', {'id': news.newsId}) }}" 
                    class="flex-1 px-6 py-3 bg-gray-700 text-white rounded-lg font-bold text-lg hover:bg-gray-600 transition text-center"
                >
                    Cancel
                </a>
            </div>

            <!-- Info Text -->
            <p class="text-gray-400 text-sm mt-6">
                * Required fields. Changes will be saved immediately.
            </p>
        </form>
    </div>
</section>
{% endblock %}
